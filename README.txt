MUMBO'S WORLD — SEPTEMBER 24 UPDATE

Apply these files to the current GitHub branch, preserving the paths shown here:
  index.html
  desktop-v4.css
  hat-layout.js
  journey.js
  reel-motion.js (new file)
  assets/media-ocean-v1.webp (new image)

The current index.html loads reel-motion.js after desktop-v3.js.

Changes:
- Media Center landing art now has a continuous ocean and curved horizon.
- GPS world names roll vertically when using arrows, wheel, drag, or keyboard.
- Bucket hat is up to 15% larger, bounded by the hanging sign and dashboard.
- INFO and EXPLORER labels are inset inside the dashboard screen.
- Arriving in a mini world delays its reveal until the zoom is nearly complete,
  with a brief cloud-like mist and easing.

This is an update package, not a full site archive. Keep every other file in
its current GitHub version. The image and JS need to be placed at the paths
shown above. Review in a Vercel preview before merging to main.
